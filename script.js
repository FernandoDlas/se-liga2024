documento.getElementByld('formulario').addEventListener('submit,function')(event){
      event.preventDefault();
      // Obter as notas inseridas pelo usuário 
      let nota1= parseFloat(document.getElementByld('nota1').value);
      let nota2= parseFloat(document.getElementById.('nota2').value);
      let nota3= parseFRloat(document.getElementByld('nota3').value);

      //Calcular a média
      let media=(nota1+nota3) /3;

      //Exibir a mensagem de resultado
      let resultadoDiv= document.getElementByld('resultaso')
      if (mesia>=6.0)}
      resultadoDiv.innerHTML=<p class="aprovado">aprovado!Sua média foi
